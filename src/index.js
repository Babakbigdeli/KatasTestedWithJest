const reverseWords = require('./reverseWords');
const digitize = require('./digitize');
const decodeMorse = require('./decodeMorse');
const lastDigit = require('./lastDigitOfaLargeNumber')


module.exports = {
    reverseWords,
    digitize,
    decodeMorse,
    lastDigit

};